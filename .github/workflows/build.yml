name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Setup .NET 4.7
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 4.7
        
      - name: Build core app
        run: dotnet build .\src\JiuLing.AutoUpgrade\JiuLing.AutoUpgrade.csproj --no-restore

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
          
      - name: Restore dependencies
        run: dotnet restore
        
      - name: Build core shell
        run: dotnet build .\src\JiuLing.AutoUpgrade.Shell\JiuLing.AutoUpgrade.Shell.csproj --no-restore
